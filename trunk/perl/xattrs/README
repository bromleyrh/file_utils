xattrs version 0.01
===================

This module provides an interface to the *xattr() system calls, if present,
allowing Perl scripts to read and manipulate files' extended attributes.

INSTALLATION

To install this module type the following:

   perl Makefile.PL
   make
   make test
   make install

